wtdttt

Description - basic wtd fit

Usage

wtdttt(formula, parameters, data, start, end, reverse, conttime,
	subset, na.action, init, control)

ranwtdttt

Description - extension to wtdttt for random start times

Usage

ranwtdttt(formula, parameters, data, id, start, end, reverse, conttime,
	nsamp=1, subset, na.action, init, control)

predict

Description - predict from fitted model (probability or duration)

Usage

predict(object, newdata = NULL,
	type = c("prob", "dur"), distrx = NULL, quantile=0.8,
	se.fit = FALSE,
	na.action = na.pass)

plot

Description - plot diagnostics for fitted model (histogram vs parametric)

Usage

plot(object)
